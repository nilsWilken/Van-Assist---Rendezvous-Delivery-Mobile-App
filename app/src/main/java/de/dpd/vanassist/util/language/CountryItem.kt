package de.dpd.vanassist.util.language

import java.util.*

class CountryItem {

    var imageFlag:Int
    var locale:Locale

    constructor(imageFlag:Int, locale:Locale) {
        this.imageFlag = imageFlag
        this.locale = locale
    }
}